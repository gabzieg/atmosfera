# --- Android / Gradle ---
*.apk
*.ap_
*.aab
*.dex
*.class
bin/
gen/
out/
build/
.gradle/
android-app/app/build/
local.properties
# Distribuição do Gradle extraída localmente (baixada sob demanda pelo wrapper
# a partir de gradle-wrapper.properties — não é código-fonte, ~140MB).
android-app/gradle-8.6/

# --- Android Studio ---
.idea/
*.iml

# --- Arquivos compactados (não versionar backups) ---
*.rar
*.zip
*.7z

# --- Python ---
__pycache__/
*.py[cod]
*$py.class
.venv/
venv/

# --- Imagens Geradas ---
# Ignoramos a pasta de output para não lotar o repositório,
# mas os assets do motor (android-app/app/src/main/assets/atmosfera/) SÃO enviados.
image-generator/output/

# --- Segurança (MUITO IMPORTANTE) ---
# ATENÇÃO: não use "*.properties" aqui — isso ignoraria gradle.properties e
# gradle-wrapper.properties (essenciais do build). Ignore só os segredos:
*.jks
*.keystore
keystore.properties
secrets.properties
local.properties
.env