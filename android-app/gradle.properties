org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
android.useAndroidX=true
# android.enableJetifier REMOVIDO na migração pro AGP 9.x (2026-09-18). Ele
# reescrevia bibliotecas legadas `android.support.*` pra AndroidX — e este
# projeto não tem nenhuma (todas as dependências já nascem AndroidX). Estava
# custando tempo de build à toa, o AGP 9 avisa que está descontinuado e o
# AGP 10.0 remove a opção. Se algum dia entrar uma lib pré-AndroidX, o sintoma
# é erro de classe `android.support.*` não encontrada no dex/merge.
kotlin.code.style=official
